import 'package:flutter/cupertino.dart';

class TimePeriodView extends StatelessWidget {
  const TimePeriodView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
