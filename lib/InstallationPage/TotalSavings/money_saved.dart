import 'package:flutter/cupertino.dart';

class MoneySaved extends StatelessWidget {
  const MoneySaved({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Money saved");
  }
}
