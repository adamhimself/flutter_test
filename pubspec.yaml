name: taylor_janus
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.18.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  fl_chart: ^0.61.0
  http: ^0.13.5

  english_words: ^4.0.0
  provider: ^6.0.5
  flutter_barcode_scanner: ^2.0.0
  gauges: ^1.0.0
  aad_oauth: ^0.4.3
  webview_flutter: ^4.2.0
  flutter_spinkit: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  lints: ^2.0.1

fonts:
  - family: SuisseIntl
    fonts:
      - asset: fonts/SuisseIntl-Regular.ttf
      - asset: fonts/SuisseIntl-Medium.ttf
      - asset: fonts/SuisseIntl-Bold.ttf

flutter:
  uses-material-design: true

  